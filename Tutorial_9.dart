void main(){
  // For Loop
  // for(var i=1; i<=10; i++){
  //   print(i);
  // }

  // For...in Loop
  // var numbers = [1, 2, 3];
  // for (var n in numbers){
  //   print(n);
  // }
  // for (var i=0; i<numbers.length; ++i){
  //   print(numbers[i]);
  // }

  // For Each Loop
  // var numbers = [1, 2, 3];
  // numbers.forEach((n)=>print(n));  // (n)=>print(n) is Arrow Function

  // While Loop
  int num = 5;
  while (num>0){
    print(num);
    num-=1;
  }

  // do...While Loop
  do{
    print(num);
    num-=1;
  }while(num>0);
}
